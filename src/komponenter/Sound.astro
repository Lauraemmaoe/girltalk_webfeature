---

---

<html>
  <img
    id="playButton"
    src="/sound.svg"
    alt="Afspil lyd"
    onclick="playAudio()"
  />

  <audio id="audioPlayer">
    <source src="ocean.mp3" type="audio/mpeg" />
    Din browser understøtter ikke afspilning af MP3-lyd.
  </audio>

  <style>
    #playButton {
      height: 10%;
      width: 5%;
      display: flex;
      align-items: end;
      justify-content: end;

      position: fixed;
      top: 20px; /* Ændr efter behov for at justere den ønskede afstand fra toppen */
      right: 30px; /* Ændr efter behov for at justere den ønskede afstand fra venstre */

      box-shadow: 0 2px 5px rgba(43, 29, 237, 0.626);
    }
  </style>

  <script>
    const playButton = document.getElementById("playButton");
    const audioPlayer = document.getElementById("audioPlayer");

    playButton.addEventListener("click", playSound);

    function playSound() {
      console.log("paused?", audioPlayer.paused);
      if (audioPlayer.paused) {
        audioPlayer.play();
      } else {
        audioPlayer.pause();
      }
    }
  </script>


</html>
